import { SearchCakeDirective } from './search-cake.directive';

describe('SearchCakeDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchCakeDirective();
    expect(directive).toBeTruthy();
  });
});
